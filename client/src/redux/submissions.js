const intialState = {
    submissions: []
}

const submissionReducer = (state = intialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default submissionReducer;